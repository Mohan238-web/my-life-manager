name: Build Corex v9.13.257 and PC Companion

on:
  push:
    branches: [ corex-pc-v9.13.257 ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: corex-v9.13.249
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"
      - name: Restore and verify the consolidated Corex HTML
        run: |
          mkdir -p app/src/main/assets
          cat app/src/main/assets_parts/index.html.gz.b64.part-* | base64 --decode | gzip --decompress > app/src/main/assets/index.html
          grep -q "const VERSION='9.13.257'" app/src/main/assets/index.html
          grep -q 'id="syncDevicesCard"' app/src/main/assets/index.html
          grep -q 'CorexPcNative' app/src/main/assets/index.html
          grep -q 'Connect securely' app/src/main/assets/index.html
      - name: Run Corex and connection regressions
        working-directory: .
        run: node tests/verify-v913251.mjs
      - name: Compile Android app without emulator
        run: gradle --no-daemon --stacktrace :app:assembleDebug
      - name: Verify APK identity and connection components
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk Corex-v9.13.257-unsigned.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose Corex-v9.13.257-unsigned.apk
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging Corex-v9.13.257-unsigned.apk | grep "package: name='com.mohan.mylifemanager' versionCode='913257' versionName='9.13.257-corex-pc'"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump permissions Corex-v9.13.257-unsigned.apk | grep 'android.permission.INTERNET'
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump xmltree Corex-v9.13.257-unsigned.apk AndroidManifest.xml | grep 'corex'
          unzip -l Corex-v9.13.257-unsigned.apk | grep 'assets/index.html'
      - uses: actions/upload-artifact@v4
        with:
          name: Corex-v9.13.257-unsigned-APK
          path: corex-v9.13.249/Corex-v9.13.257-unsigned.apk
          if-no-files-found: error
          retention-days: 30
      - name: Prepare Android signing verifier
        run: |
          APKSIGNER_JAR=$(find "$ANDROID_HOME/build-tools" -path '*/lib/apksigner.jar' | sort -V | tail -1)
          test -f "$APKSIGNER_JAR"
          cp "$APKSIGNER_JAR" Corex-apksigner.jar
      - uses: actions/upload-artifact@v4
        with:
          name: Corex-apksigner-tool-v913257
          path: corex-v9.13.249/Corex-apksigner.jar
          if-no-files-found: error
          retention-days: 1

  windows-companion:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: pc-companion
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"
          cache-dependency-path: pc-companion/go.mod
      - name: Test encrypted pairing, synchronization and QR generation
        run: go test -v ./...
      - name: Build one standalone Windows EXE
        shell: cmd
        run: build-windows.bat
      - name: Verify native EXE
        shell: pwsh
        run: |
          $file = Get-Item Corex-PC-Companion-v1.0.0.exe
          if ($file.Length -lt 1000000 -or $file.Length -gt 30000000) { throw "Unexpected EXE size: $($file.Length)" }
          $bytes = [System.IO.File]::ReadAllBytes($file.FullName)
          if ($bytes[0] -ne 0x4d -or $bytes[1] -ne 0x5a) { throw "Not a Windows PE executable" }
          Get-FileHash $file.FullName -Algorithm SHA256
      - uses: actions/upload-artifact@v4
        with:
          name: Corex-PC-Companion-v1.0.0-Windows
          path: pc-companion/Corex-PC-Companion-v1.0.0.exe
          if-no-files-found: error
          retention-days: 30
