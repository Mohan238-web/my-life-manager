name: Probe PhoneBridge Windows Microphone Driver

on:
  push:
    branches: [phonebridge-build]
    paths:
      - '.github/workflows/probe-phonebridge-mic-driver.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Clone Microsoft Windows driver samples
        shell: pwsh
        run: git clone --depth 1 https://github.com/microsoft/Windows-driver-samples.git MicrosoftDriverSamples

      - name: Inject current WDK NuGet package into official sample
        shell: pwsh
        run: |
          $props = @'
          <Project>
            <ItemGroup>
              <PackageReference Include="Microsoft.Windows.WDK.x64" Version="10.0.28000.2526" />
            </ItemGroup>
          </Project>
          '@
          $props | Set-Content MicrosoftDriverSamples/audio/simpleaudiosample/Directory.Build.props

      - name: Restore WDK packages
        shell: pwsh
        run: |
          $vswhere="${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $msbuild=& $vswhere -latest -products * -requires Microsoft.Component.MSBuild -find MSBuild\**\Bin\MSBuild.exe | Select-Object -First 1
          if(-not $msbuild){ throw 'MSBuild not found' }
          & $msbuild MicrosoftDriverSamples/audio/simpleaudiosample/SimpleAudioSample.sln /m /t:Restore /p:Configuration=Release /p:Platform=x64 /p:RestorePackagesConfig=true /v:minimal
          if($LASTEXITCODE -ne 0){ throw 'WDK NuGet restore failed' }

      - name: Locate compatible WDK command-line tools
        shell: pwsh
        run: |
          $root=Join-Path $env:USERPROFILE '.nuget\packages\microsoft.windows.wdk.x64\10.0.28000.2526'
          $stampCandidates=Get-ChildItem $root -Recurse -Filter stampinf.exe -ErrorAction SilentlyContinue
          $stamp=$stampCandidates | Where-Object { $_.FullName -match '\\x64\\stampinf\.exe$' } | Select-Object -First 1
          if(-not $stamp){ $stamp=$stampCandidates | Where-Object { $_.FullName -match '\\x86\\stampinf\.exe$' } | Select-Object -First 1 }
          if(-not $stamp){ throw 'Compatible stampinf.exe not found in WDK NuGet package' }
          Write-Host "Using stampinf: $($stamp.FullName)"
          $stamp.DirectoryName | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
          $inf2cat=Get-ChildItem $root -Recurse -Filter Inf2Cat.exe -ErrorAction SilentlyContinue | Where-Object { $_.FullName -match '\\x64\\Inf2Cat\.exe$|\\x86\\Inf2Cat\.exe$' } | Select-Object -First 1
          if($inf2cat){ Write-Host "Using Inf2Cat: $($inf2cat.FullName)"; $inf2cat.DirectoryName | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8 }
          Write-Host 'All stampinf candidates:'
          $stampCandidates | Select-Object -ExpandProperty FullName

      - name: Build official Simple Audio Sample x64
        shell: pwsh
        run: |
          $vswhere="${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $msbuild=& $vswhere -latest -products * -requires Microsoft.Component.MSBuild -find MSBuild\**\Bin\MSBuild.exe | Select-Object -First 1
          & $msbuild MicrosoftDriverSamples/audio/simpleaudiosample/SimpleAudioSample.sln /m /t:Build /p:Configuration=Release /p:Platform=x64 /v:minimal
          if($LASTEXITCODE -ne 0){ throw 'SimpleAudioSample build failed after WDK NuGet restore/tools setup' }

      - name: Show driver outputs
        shell: pwsh
        run: |
          Get-ChildItem MicrosoftDriverSamples/audio/simpleaudiosample -Recurse -Include *.sys,*.inf,*.cat,*.cer | Select-Object FullName,Length

      - uses: actions/upload-artifact@v4
        with:
          name: Microsoft-SimpleAudioSample-WDK-NuGet-Probe
          path: |
            MicrosoftDriverSamples/audio/simpleaudiosample/**/package/**
            MicrosoftDriverSamples/audio/simpleaudiosample/**/*.sys
            MicrosoftDriverSamples/audio/simpleaudiosample/**/*.inf
            MicrosoftDriverSamples/audio/simpleaudiosample/**/*.cat
            MicrosoftDriverSamples/audio/simpleaudiosample/**/*.cer
          if-no-files-found: warn
